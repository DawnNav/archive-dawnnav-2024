# 黎明导航项目实施说明书

这是一份可直接交给 AI 编程代理执行的项目说明书。目标不是继续修补旧主题，而是先分析现有站点的功能和逻辑，再定义一个更现代、更易维护、支持多语言和后台管理的导航站模板。

如果 AI 需要开工，请默认按本说明书执行。只有遇到信息绝对缺失、且会影响实现正确性的地方，才允许先提问。

## 0. 现有站点分析

当前仓库是一个基于 Hugo WebStack 风格的导航站，内容主题比较鲜明，页面结构也比较完整，但整体技术和交互已经明显过时。它的价值在于说明了站点应该长什么样、有哪些内容类型、哪些交互是必须保留的。

### 0.1 现有站点在做什么

- 首页是分类导航加链接卡片布局。
- 内容主要来自 `data/webstack.yml` 和少量 Markdown 页面。
- 有分类锚点导航、站内搜索入口、跳转页、暗黑模式、编辑按钮和天气模块。
- 有中英页面，但本质上不是完整的多语言系统，只是分散的内容文件。
- 链接卡片支持标题、简介、logo、二维码等基本信息。
- 站点更像“静态导航页”，不是“可运营的资源目录平台”。

### 0.2 现有逻辑的关键路径

- `data/webstack.yml` 决定首页所有分类和链接数据。
- `themes/hugo-webstack/layouts/index.html` 负责分类目录、链接卡片、侧边栏、页脚动作的渲染。
- `themes/hugo-webstack/layouts/goto/single.html` 和 `static/js/goto.js` 负责外链中转和跳转体验。
- `hugo.toml` 里可以看到搜索、暗黑模式、在线编辑、pinyin 搜索等开关，但多数能力偏配置化，不是可管理的业务能力。

### 0.3 现有站点的主要问题

- 技术栈老旧，Hugo + jQuery + Bootstrap 3 的组合已经不适合新模板。
- 结构过于依赖单个 YAML 文件，不利于增删改和批量维护。
- 多语言不统一，英文内容不是完整的 i18n 设计。
- 缺少真正的后台管理，不方便站长持续运营。
- 搜索、状态管理、点击统计、失效检测都比较弱。
- 视觉上能用，但不够现代，移动端体验也还有很多改进空间。

### 0.4 结论

这个项目不是“把旧 Hugo 主题改漂亮一点”，而是要重新定义成一个现代化导航站模板，保留原有内容组织逻辑，但把信息架构、数据模型、后台能力和部署方式重新搭建。

## 1. 项目目标

你要创建的是一个现代化、多语言、可运营、可持续维护的导航网站。它不是纯静态链接页，而是一个资源目录平台，具备首页展示、分类浏览、站点详情、全文搜索、多语言切换、站点提交、后台管理、数据导入、统计与审核能力。

### 1.1 核心目标

- 支持中文、英文，并为未来增加更多语言预留结构。
- 支持每个导航条目有多语言标题、简介、标签和页面文案。
- 支持网站名称、网站介绍、RSS、图标、备用网址和维护状态。
- 支持分类、专题、标签、友链、收藏、失效反馈和审核状态。
- 支持搜索优先的首页设计，同时保留分类浏览体验。
- 支持后台管理和内容导入，不要求一切只能手写代码改数据。
- 支持部署到 Cloudflare Pages、Vercel，或独立服务器。

### 1.2 设计原则

- 先可用，再完善，再美化。
- 内容结构优先于视觉堆砌。
- 所有功能都要考虑移动端。
- 多语言不是翻译附加项，而是核心信息架构的一部分。
- 新项目要避免把业务逻辑散落在几十个小脚本里。

### 1.3 交付标准

AI 最终应输出一个可以启动、可以编辑、可以部署的完整项目，至少包含：

- 前台网站。
- 后台管理页面。
- API 或 server actions。
- 数据库模型和迁移方案。
- 多语言支持。
- 搜索支持。
- 导入导出支持。
- README 和部署说明。

## 2. 最终产品定义

### 2.1 产品定位

这是一个导航网站、资源目录站、专题聚合站和轻量内容运营平台的结合体。

### 2.2 用户角色

- 访客：浏览、搜索、切换语言、跳转外站。
- 注册用户：收藏、反馈失效、提交站点。
- 编辑：维护分类、站点、专题、翻译。
- 管理员：管理权限、审核内容、导入导出、查看统计。

### 2.3 用户核心任务

- 快速找到某个资源。
- 通过语言切换查看对应文案。
- 浏览分类目录。
- 查看站点详情、备用网址、RSS、图标、标签。
- 提交新站点或修正失效信息。
- 后台批量维护内容。

## 3. 技术选型要求

AI 请默认使用以下技术栈实现，除非某项在运行环境中不可行：

- 框架：Next.js 15 或更高，使用 App Router。
- 语言：TypeScript。
- 样式：Tailwind CSS。
- UI 组件：shadcn/ui。
- 图标：Lucide React。
- 表单：React Hook Form + Zod。
- 数据库：PostgreSQL。
- ORM：Drizzle ORM 或 Prisma，二选一即可，优先选 AI 最容易稳定实现的方案。
- 认证：NextAuth.js 或等价实现。
- 搜索：优先实现数据库全文检索，后续可扩展 Meilisearch。
- 部署：Cloudflare Pages 优先，Vercel 兼容，服务器部署作为可选方案。

### 3.1 默认工程约束

- 使用 App Router，不使用旧 pages router。
- 使用函数组件和 React Hooks。
- 使用 Server Components / Server Actions 作为默认方式，但需要明确区分客户端组件。
- 页面与数据访问分层，不要把数据库逻辑写进纯 UI 组件里。
- 代码必须保持可读、可维护，不要依赖神秘黑魔法。

### 3.2 不要做的事

- 不要继续使用 Hugo、jQuery、Bootstrap 3 作为主方案。
- 不要把所有内容继续堆在单个 YAML 文件里。
- 不要把国际化做成“只有首页有英文标题”的假翻译。
- 不要把后台做成只能手动改源码的工具。

## 4. 信息架构

### 4.1 必要页面

AI 至少要创建以下页面：

- 首页 `/`
- 分类页 `/category/[slug]`
- 站点详情页 `/site/[slug]`
- 专题页 `/collection/[slug]`
- 标签页 `/tag/[slug]`
- 搜索页 `/search`
- 提交站点页 `/submit`
- 关于页 `/about`
- 站点政策页 `/policy` 或 `/disclaimer`
- 跳转页 `/goto/[id]` 或等价中转页
- 后台首页 `/admin`
- 后台站点管理 `/admin/sites`
- 后台分类管理 `/admin/categories`
- 后台专题管理 `/admin/collections`
- 后台翻译管理 `/admin/translations`
- 后台导入导出 `/admin/import-export`
- 后台审核 `/admin/reviews`
- 后台设置 `/admin/settings`

### 4.2 路由国际化

语言前缀必须支持，例如：

- `/zh`
- `/en`
- `/ja`

默认语言可以是中文或英文，由配置决定，但必须明确并统一。

### 4.3 页面类型

- 落地页：面向搜索和入口。
- 列表页：分类和专题。
- 详情页：站点详细信息。
- 系统页：后台管理、导入、设置。
- 静态页：关于、政策、联系、免责声明。

## 5. 视觉与交互要求

### 5.1 视觉方向

要求是现代、克制、信息密度高但不拥挤。不要做成老式导航站，也不要做成花哨但难用的 AI 风格网站。

### 5.2 首屏要求

- 顶部必须有品牌区和语言切换器。
- 首屏必须有一个强视觉的搜索框。
- 搜索框下方应有精选分类、热门站点或快捷入口。
- 首页不能只是一堆卡片直接铺满。

### 5.3 导航与筛选

- 分类导航要清晰。
- 要支持标签筛选。
- 要支持排序，例如按推荐、最新、最热、字母序。
- 要支持状态筛选，例如正常、待审、失效、已下线。

### 5.4 卡片样式

- 卡片要有标题、简介、logo、RSS、标签和语言标记。
- 若站点有多语言内容，应显示当前语言对应的标题和简介。
- 若某语言缺失，要显示回退内容，并提示“暂无该语言翻译”。
- 支持收藏、复制链接、打开详情、外链跳转。

### 5.5 动效

- 动效要克制，最多用于页面进入、搜索切换、展开折叠和状态变化。
- 不要堆砌无意义的炫技动画。

## 6. 数据模型设计

### 6.1 必要实体

AI 需要设计以下核心数据模型：

- `Locale`
- `Category`
- `CategoryTranslation`
- `Site` 或 `Link`
- `SiteTranslation` 或 `LinkTranslation`
- `Tag`
- `Collection`
- `CollectionItem`
- `Page`
- `PageTranslation`
- `User`
- `Role`
- `Permission`
- `Review`
- `ClickEvent`
- `Favorite`
- `Submission`
- `RedirectLog`
- `ImportJob`
- `AuditLog`

### 6.2 站点条目字段

每个站点条目至少要有：

- `id`
- `slug`
- `status`
- `primaryUrl`
- `title`
- `description`
- `rssUrl`
- `logoUrl`
- `screenshotUrl`
- `language`
- `languagesSupported`
- `countryOrRegion`
- `tags`
- `categoryId`
- `collectionIds`
- `isFeatured`
- `isPinned`
- `isExternalRedirectEnabled`
- `createdAt`
- `updatedAt`

### 6.3 多语言字段设计

不要只把英文内容塞进一个英文 markdown 页面。应采用以下方式之一：

#### 方案 A：翻译表

每个基础实体单独维护翻译表。

- `title` 存默认语言。
- 翻译表存其他语言的标题、简介、标签、说明。

优点：结构清晰，适合多语言扩展。

#### 方案 B：i18n JSON 字段

例如：

```json
{
  "title_i18n": {
    "zh": "中文标题",
    "en": "English Title"
  },
  "description_i18n": {
    "zh": "中文简介",
    "en": "English description"
  }
}
```

优点：实现简单。

#### 推荐

优先采用翻译表方式，前台渲染时按当前语言读取翻译；缺失则回退到默认语言。

### 6.4 回退规则

必须明确回退链：

1. 当前语言。
2. 默认语言。
3. 通用文本或空状态。

如果某个条目只有中文简介，没有英文简介，英文页面必须显示中文简介或明确的“暂无英文简介”。不能默默空白。

## 7. 功能需求

### 7.1 首页

首页必须具备：

- 品牌区。
- 语言切换器。
- 全局搜索。
- 推荐分类。
- 热门站点。
- 最近更新。
- 专题精选。
- RSS 提示或入口。
- 失效提示或公告区。

首页要支持按语言动态显示文案和内容。

### 7.2 搜索

搜索功能必须支持：

- 标题搜索。
- 简介搜索。
- 标签搜索。
- 域名搜索。
- 分类搜索。
- RSS 搜索。
- 拼音搜索可选。
- 英文、中文、其他语言混合搜索。

搜索结果应支持：

- 高亮命中。
- 排序。
- 多条件筛选。
- 键盘操作。

### 7.3 分类浏览

分类页必须支持：

- 分类简介。
- 分类图标。
- 子分类。
- 条目排序。
- 标签过滤。
- 语言过滤。

### 7.4 站点详情页

详情页必须展示：

- 当前语言标题。
- 当前语言简介。
- 全部可用语言的切换入口。
- 网站原始 URL。
- RSS。
- logo 和截图。
- 标签。
- 分类与专题关系。
- 备用网址。
- 语言支持信息。
- 最近更新或维护状态。
- 外链跳转按钮。

### 7.5 跳转页

跳转页是可选增强，但建议保留。

要求：

- 显示目标站点标题和简介。
- 支持倒计时跳转。
- 支持立即前往、返回上一页、复制链接。
- 如果站点存在多语言描述，跳转页要展示当前语言版本。

### 7.6 提交与反馈

用户应能提交：

- 新站点。
- 修正链接。
- 补充翻译。
- 报告失效。

后台应能审核这些提交。

### 7.7 收藏与个性化

如果时间允许，最好支持：

- 收藏站点。
- 最近访问。
- 个人偏好语言。
- 个人偏好排序。

这部分可以做成第二阶段功能，但数据结构要预留。

## 8. 后台管理需求

### 8.1 站点管理

后台应支持：

- 新增站点。
- 编辑站点。
- 删除站点。
- 上线、下线、审核中、失效。
- 置顶、推荐、隐藏。
- 批量导入。
- 批量导出。

### 8.2 分类管理

- 分类新增、编辑、删除。
- 分类排序。
- 分类图标。
- 分类描述。
- 分类多语言翻译。

### 8.3 专题管理

- 创建专题。
- 专题下挂多个站点。
- 专题也要支持多语言。

### 8.4 翻译管理

必须提供一个专门的翻译管理模块，能看到：

- 哪些分类缺英文。
- 哪些站点缺英文。
- 哪些页面缺翻译。
- 翻译完成率。

### 8.5 导入导出

必须支持导入旧数据，至少支持：

- YAML。
- JSON。
- CSV。

导入时要能识别：

- 分类。
- 条目。
- 语言字段。
- 友链。
- 图片地址。
- RSS 地址。
- 状态字段。

### 8.6 审核与审计

后台操作必须可追踪：

- 谁改了什么。
- 什么时候改的。
- 改动前后是什么。

## 9. 国际化要求

这是本项目的重点之一。

### 9.1 语言切换行为

- 页面顶部固定一个语言切换器。
- 切换后保持当前页面语义，例如同一个站点切语言后仍在同一个详情页，只是文案变化。
- 切换语言后，如果当前页面没有对应翻译，必须回退到默认语言内容。

### 9.2 URL 规则

推荐采用语言前缀路由：

- `/zh`
- `/en`
- `/ja`

如果未来扩展更多语言，继续按前缀扩展。

### 9.3 SEO 规则

- 每个语言版本要有自己的 `title` 和 `description`。
- 页面要输出 `hreflang`。
- 页面要有正确的 `canonical`。
- sitemap 要包含多语言页面。
- 站点元数据必须按语言生成。

### 9.4 UI 规则

- 当前语言要在 UI 中明确可见。
- 所有按钮和标签都要按语言切换。
- 语言代码不要只在后台存在，前台用户要能感知。

### 9.5 缺失翻译处理

- 缺翻译时显示回退内容。
- 缺翻译时标记状态。
- 后台显示未翻译条目列表。

## 10. 页面级实现建议

### 10.1 首页

组件建议：

- `Header`
- `LanguageSwitcher`
- `HeroSearch`
- `FeaturedCategories`
- `TrendingSites`
- `RecentUpdates`
- `CollectionsPreview`
- `Footer`

### 10.2 列表页

组件建议：

- `PageTitle`
- `Breadcrumbs`
- `FilterBar`
- `SortBar`
- `LanguageFilter`
- `SiteGrid`
- `Pagination` 或 `InfiniteScroll`

### 10.3 详情页

组件建议：

- `SiteHero`
- `SiteLanguageTabs`
- `SiteDescription`
- `SiteMetadata`
- `RelatedSites`
- `ExternalLinks`
- `ReportPanel`

### 10.4 后台页

组件建议：

- `AdminLayout`
- `AdminSidebar`
- `DataTable`
- `EntityForm`
- `TranslationEditor`
- `ImportWizard`
- `AuditTimeline`

## 11. 代码结构要求

AI 应在项目根目录建立清晰结构，至少包含类似下面的分层：

```text
app/
components/
features/
lib/
server/
db/
messages/
public/
styles/
scripts/
types/
```

### 11.1 建议职责

- `app/`：路由和页面。
- `components/`：通用 UI 组件。
- `features/`：按业务域拆分的逻辑，例如站点管理、分类管理、翻译管理。
- `lib/`：工具函数、常量、格式化、校验。
- `server/`：服务端业务逻辑。
- `db/`：数据库 schema、迁移、seed。
- `messages/`：多语言文案文件。
- `public/`：静态资源。
- `types/`：共享类型。

### 11.2 不要把所有逻辑都放进一个文件

如果 AI 生成的项目仍然是“页面里直接写全部逻辑”，说明没有达到工程级别。

## 12. 数据迁移要求

### 12.1 旧数据来源

旧项目中主要可迁移内容包括：

- `data/webstack.yml`
- `content/en.md`
- `content/about.md`
- `content/goto/*`
- 旧编辑逻辑和跳转逻辑

### 12.2 迁移方式

- 先编写导入脚本。
- 将旧 YAML 转成数据库初始数据。
- 将中英文页面内容导入新模型。
- 将图片、logo、二维码、RSS 等资源映射到新存储位置。

### 12.3 迁移验收

- 导入后能正常显示。
- 旧分类不丢失。
- 旧链接不丢失。
- 中英文内容能正确对应。

## 13. 性能与质量要求

### 13.1 性能

- 首页首屏要快。
- 搜索要快。
- 列表页要快。
- 详情页要快。
- 大量链接情况下不能明显卡顿。

### 13.2 可访问性

- 按钮和输入框应有清晰标签。
- 键盘可操作。
- 颜色对比要足够。
- 移动端可用。

### 13.3 容错

- 图片加载失败要有默认图。
- 链接失效要有提示。
- 数据缺失要有空状态。
- 语言缺失要有回退。

## 14. 测试要求

AI 需要在项目中加入至少以下测试思路：

- 单元测试：校验函数、回退逻辑、数据转换。
- 集成测试：API、导入、搜索、语言切换。
- E2E 测试：首页浏览、切换语言、打开详情页、后台提交。

如果时间不足，至少要保证关键数据层逻辑有测试。

## 15. 部署要求

### 15.1 Cloudflare Pages

优先支持静态或半静态部署，并确保环境变量配置清晰。

### 15.2 Vercel

作为备用部署平台也要可用。

### 15.3 服务器部署

如果选择独立服务器部署，也要保持以下能力：

- 前台可单独构建。
- 后台可独立访问。
- API 可与前台分离。
- 数据库和对象存储可独立配置。

### 15.4 环境变量

必须文档化，如：

- 数据库连接。
- 认证密钥。
- 存储服务密钥。
- 搜索服务配置。
- 站点基础 URL。
- 站点默认语言。

## 16. 开发流程要求

AI 必须按以下顺序推进，而不是随手写页面：

1. 建立项目骨架。
2. 建立配置和基础依赖。
3. 定义数据库模型。
4. 实现基础 UI 组件。
5. 实现首页和列表页。
6. 实现多语言系统。
7. 实现搜索和详情页。
8. 实现后台管理。
9. 实现导入导出和审核。
10. 加测试、加文档、加部署配置。

## 17. 交付物清单

AI 最终应该至少输出：

- 一个完整的 Next.js 项目。
- 一个清晰的 README。
- 一个数据库 schema。
- 一个导入脚本或迁移脚本。
- 一个多语言方案。
- 一个可运行的后台管理界面。
- 一套可用的前台导航页面。
- 一份部署说明。

## 18. 验收标准

项目完成后，至少应满足以下验收条件：

- 项目能启动。
- 首页可访问。
- 语言切换可用。
- 中文和英文内容能分别显示。
- 站点详情页可用。
- 搜索可用。
- 后台可用。
- 旧数据可导入。
- 基础 SEO 信息存在。
- 页面在手机和桌面端都正常。

## 19. 给 AI 的执行说明

如果你把这份文档交给另一个 AI，请直接告诉它：

- 这是一个从零开始的新项目，不是旧项目修复。
- 先搭项目骨架，再做数据模型，再做页面，再做后台。
- 语言切换是核心需求，不能后补。
- 条目级多语言内容是核心需求，不能只做页面翻译。
- RSS、图标、备用网址和翻译字段必须纳入模型。
- 数据结构必须支持未来扩展。
- 不要来回问太多问题，默认按本说明书执行。

## 20. 建议的默认实现顺序

如果 AI 需要一个最稳妥的执行顺序，建议按下面来：

1. 初始化 Next.js + TypeScript + Tailwind + shadcn/ui。
2. 建立语言配置和消息文件。
3. 建立数据库 schema。
4. 写导入旧数据脚本。
5. 做首页、分类页、详情页。
6. 做语言切换和回退。
7. 做搜索。
8. 做后台 CRUD。
9. 做审核、导入导出、翻译管理。
10. 做测试、部署和文档。

## 21. AI coding 提示词包

下面是建议直接交给 AI 编程代理的提示词结构。你可以把它们当作一份从分析到实现的完整任务书。

### 21.1 总提示词

```markdown
你现在要从零开始实现一个现代化、多语言、可运营的导航站模板。不要在旧 Hugo 主题上做小修小补，也不要只做一个静态卡片页。

请先基于以下目标完成系统设计，再进入实现：

1. 分析现有站点的功能和逻辑，保留它的内容组织思想，但不要沿用过时技术栈。
2. 设计一个支持中文、英文、未来更多语言的导航站模板。
3. 设计站点信息模型，至少包含站点名称、网站介绍、RSS、图标、备用网址、多语言名称、多语言介绍、分类、标签、状态、排序、审核状态、点击统计、更新时间。
4. 设计前台页面：首页、分类页、标签页、站点详情页、搜索页、跳转页、关于页。
5. 设计后台页面：登录、站点管理、分类管理、标签管理、专题管理、翻译管理、导入导出、审核、设置。
6. 设计部署方案：优先兼容 Cloudflare Pages 和 Vercel，也要考虑服务器部署。
7. 输出清晰的原型说明、功能说明、模块说明、技术说明和数据模型说明。
8. 接着按分阶段方式实现，先骨架、再数据、再页面、再后台、再导入导出、再测试、再文档。

实现要求：
- 使用 Next.js App Router + TypeScript。
- 使用 Tailwind CSS + shadcn/ui。
- 使用 PostgreSQL。
- 使用现代化、简洁、信息密度高的视觉风格。
- 所有页面都必须考虑移动端。
- 多语言必须是条目级的，不只是页面级标题翻译。
- 后台必须支持站长增删改和导入导出。
- 不要输出空壳页面，要做可运行、可维护、可部署的完整项目。

如果存在不确定性，请优先做出合理默认值，并在 README 中标明。除非缺失信息会直接影响实现正确性，否则不要反复提问。
```

### 21.2 第一阶段提示词：项目骨架与规范

```markdown
请先搭建项目骨架，不要急着写完整业务。

你需要完成：
- 初始化 Next.js 15+、TypeScript、Tailwind CSS、shadcn/ui。
- 建立项目目录结构、代码规范、环境变量示例、基础 README。
- 建立国际化目录和消息文件结构。
- 建立数据库连接和 ORM 基础。
- 建立基础布局、导航栏、语言切换器、页脚等通用组件。

请同时输出：
- 项目目录结构说明。
- 技术栈说明。
- 开发约定。
- 未来页面和模块的拆分建议。
```

### 21.3 第二阶段提示词：数据模型与迁移

```markdown
请设计并实现导航站的数据模型与迁移能力。

必须包含：
- 站点、分类、标签、专题、页面、翻译、用户、角色、审核、点击、收藏、提交、导入任务、审计日志。
- 站点字段必须包含网站名称、网站介绍、RSS、图标、备用网址、状态、排序、分类、标签、多语言内容。
- 设计从旧 YAML 数据导入到数据库的脚本。
- 设计导入验证、去重、字段映射、错误报告。

请输出：
- 数据表结构。
- 字段说明。
- 翻译表或 i18n 字段方案。
- 导入脚本设计。
- 迁移验收标准。
```

### 21.4 第三阶段提示词：前台原型与页面

```markdown
请实现前台导航站原型。

必须包含：
- 首页：品牌区、搜索框、精选分类、热门站点、最近更新、专题入口。
- 分类页：分类介绍、筛选、排序、站点列表。
- 站点详情页：多语言标题与介绍、RSS、图标、备用网址、标签、分类关系、跳转按钮。
- 标签页和专题页。
- 跳转页。
- 关于页与政策页。

视觉要求：
- 现代、克制、信息清晰、移动端友好。
- 不要做成老式导航页。
- 不要过度动画。

请输出：
- 页面原型说明。
- 组件拆分。
- 交互说明。
- 响应式布局规则。
```

### 21.5 第四阶段提示词：后台管理

```markdown
请实现后台管理系统。

必须包含：
- 登录与权限控制。
- 站点增删改查。
- 分类与标签管理。
- 专题管理。
- 多语言内容编辑。
- 导入导出。
- 审核与审计日志。
- 失效检测与状态标记。

后台必须支持站长日常维护，不允许只靠改源码。
```

### 21.6 第五阶段提示词：搜索、部署与质量

```markdown
请补全搜索、部署和质量保障。

必须包含：
- 全文搜索。
- 中文和英文混合搜索。
- 可选拼音搜索。
- Cloudflare Pages 与 Vercel 兼容部署配置。
- 服务器部署说明。
- 单元测试、集成测试、E2E 测试。
- README 和部署说明。

请明确哪些能力在构建时完成，哪些能力依赖数据库，哪些能力需要后台服务。
```

### 21.7 反跑偏约束

```markdown
以下内容不要做：
- 不要继续使用 Hugo、jQuery、Bootstrap 3 作为主实现。
- 不要把内容继续堆在单个 YAML 文件里。
- 不要把多语言当成首页文案翻译。
- 不要把后台做成只能手动改源码的编辑器。
- 不要输出只有 UI 没有数据模型和管理能力的半成品。
```

## 22. 最后要求

不要只做一个“看起来像导航站”的壳。这个项目必须能够真实运营、持续维护、持续添加内容，并且能在多语言场景下稳定工作。